{
  "name": "kochegar",
  "version": "1.0.0",
  "private": true,
  "description": "Kochegar — учёт заявок (Capacitor wrapper)",
  "scripts": {
    "build": "echo \"static web app — nothing to build\""
  },
  "dependencies": {
    "@capacitor/android": "^5.7.8",
    "@capacitor/core": "^5.7.8"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.8"
  }
}
